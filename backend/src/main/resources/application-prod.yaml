app:
  frontend:
    url: https://bank-frontend-l654.onrender.com

spring:
  datasource:
    url: jdbc:postgresql://dpg-d7vmq7lb910c73d5uf2g-a.oregon-postgres.render.com:5432/db_bank_amrv?sslmode=require
    username: db_bank_amrv_user
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  flyway:
    baseline-on-migrate: true
    baseline-version: "0"

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgresSQLDialect
        connection:
          useUnicode: true
          characterEncoding: UTF-8
          charSet: UTF-8